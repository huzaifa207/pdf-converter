import { writable } from "svelte/store";

export const bottomNav = writable(0)
export const profileNav = writable(null)
export const homeNav = writable(null)
export const settingsNav = writable(false)